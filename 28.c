#include <stdio.h>#include <math.h>float funck(float x, float y){float z=0;if (x*y*y<2){  if ((1-sin(x*x+y*y*y))<sqrt(x*x)){    z= (1-sin(x*x+y*y*y));  }  else {    z=sqrt(x*x);  }}else {  z=sqrt(x*x*y+8); }return z;}int main(){float sumz=0;for (float x=0.2; x<=1; x+=0.5){  for (float y=1; y<=3; y+=1.2){    if (funck(x,y)>0.2000001){      sumz+=funck(x,y);    }    printf("При x=%f и y=%f z=%f\n",x,y,funck(x,y));   }} printf("Z=%f\n",sumz);return 0;}